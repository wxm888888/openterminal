# Core dependencies
openai>=1.0.0
tqdm>=4.65.0
tiktoken>=0.5.0

# Web scraping
selenium>=4.0.0
brotli>=1.0.9
PySocks>=1.7.1

# Optional: for cast to gif conversion
# Requires agg (install separately: npm install -g @asciinema/agg)
