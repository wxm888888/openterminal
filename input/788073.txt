dsleung@vllm-demo:~$ cat request.json | jq
{
  "model": "deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B",
  "messages": [
    {
      "role": "user",
      "content": "Explain DeepSeek-R1 to a non-technical audience in 100-150 words."
    }
  ],
  "max_tokens": 1024,
  "temperature": 0.6
}
dsleung@vllm-demo:~$ curl -s -XPOST \
    -H "Authorization: Bearer $OPENAI_API_KEY" \
    -H "Content-Type: application/json" \
    -d "$(cat request.json)" \
    http://localhost:8000/v1/chat/completions \
    > response.json
dsleung@vllm-demo:~$ cat response.json | \
    jq --raw-output '.choices[0].message.content' | \
    tail -1
DeepSeek-R1 is a smart AI created by DeepSeek, designed to solve complex STEM problems. It's like a super-smart assistant that can handle a lot of tasks, from solving math problems to analyzing data and even writing research papers. It works by learning from data and real-world challenges, improving over time. DeepSeek-R1 is user-friendly, making it easy for anyone to use. Whether you're a student, a scientist, or just curious, it can help with a wide range of tasks.
dsleung@vllm-demo:~$ exit
exit
